---
milestone: M0011
reviewId: HR-M0011-01
reviewClass: release-readiness
status: pending
waiverAllowed: false
subject: YADG 1.0.0 release candidate package, consumer evidence, and public documentation
---

# HR-M0011-01 — YADG 1.0.0 Release Candidate

## Owner

M0011 — V1.0 release readiness.

## Review subject

The exact `Yadg` version `1.0.0` `.nupkg` identified by the M0011 release evidence, together with:

- package-content/metadata inspection;
- installed-tool Tier-4 evidence;
- M0010 real-runtime compatibility evidence on the release-candidate revision;
- `README.md`;
- `CHANGELOG.md`;
- NuGet pack/publish scripts;
- release evidence manifest.

## Required evidence

Before review, implementation provides:

- repository revision;
- package SHA-256;
- package ID/version;
- package-content inspection result;
- package metadata including the package readme;
- full MSBuild/.NET/Windows provenance;
- Microsoft Word and LibreOffice versions;
- successful `./eng/validate.ps1`;
- successful `./eng/test-m0010-tier3.ps1`;
- successful `./eng/test-m0011-tier4.ps1`;
- installed `yadg --version` output;
- installed-tool command/render/publish smoke evidence;
- local-only publish-script test evidence;
- documentation-audit result;
- explicit statement that no external NuGet push/GitHub Release/tag occurred;
- explicit package licensing-metadata state.

## Human acceptance

The reviewer confirms that:

- the package is named `Yadg` and versioned `1.0.0`;
- installation/invocation as command `yadg` is understandable and evidenced;
- package contents are limited to the intended tool/runtime dependencies and include required generated Office interop assemblies;
- the package does not contain tests, confidential fixtures/content, credentials, repository review evidence, or unrelated build artifacts;
- the package/readme metadata is suitable for the intended distribution surface;
- `README.md` accurately describes current V1 behavior, prerequisites, limitations, renderer differences, publishing, and trust boundaries without relying on milestone history;
- `CHANGELOG.md` contains a useful `1.0.0` entry;
- CLI help/version and documentation agree;
- the NuGet publish script requires an explicit source and does not embed credentials or automatically publish externally;
- the absence/presence of package license metadata matches explicit project authority and is consciously accepted for this release candidate;
- the release evidence represents the exact reviewed package hash;
- the release candidate is suitable to move to a separately authorized publication action.

## Decisions

Acceptable terminal decisions:

```text
approved
changes-requested
rejected
```

Waiver is forbidden.

## Completion gate

M0011 cannot be complete until:

```powershell
./eng/review-check.ps1 --milestone M0011
```

passes against an approved review record for the exact release-candidate evidence/package hash.
